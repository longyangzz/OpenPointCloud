PROJECT(las2pdb)

SET(OpenPointCloud_APPLICATION_DIR ${PROJECT_SOURCE_DIR})

INCLUDE_DIRECTORIES(
		${QT_INCLUDE_DIR} 
		${QT_QTCORE_INCLUDE_DIR}
		${QT_QTGUI_INCLUDE_DIR}
	)

SET(TARGET_DEFAULT_PREFIX "tool_")
SET(TARGET_DEFAULT_LABEL_PREFIX "Tool")


ADD_SUBDIRECTORY(las2pdb)